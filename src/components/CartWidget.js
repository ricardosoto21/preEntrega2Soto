import React from 'react';

const CartWidget = () => {
    return (
        <div>
            <i className="fas fa-shopping-cart"></i>
            <span className="badge badge-primary">5</span> // (número hardcodeado)
        </div>
    );
}

export default CartWidget;